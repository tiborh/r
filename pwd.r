Sys.getenv("PWD")                       # present working directory
                                        # if one member is symbolic link,
                                        # then symbolic name will appear
getwd()                                 # working directory: no symlink names
